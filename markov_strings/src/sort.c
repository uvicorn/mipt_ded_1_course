int comparator(const char* a, const char* b){
    while (*a && *b && (*a == *b) ){
        a++;
        b++;
        while (strchr(a))
    }
    return *(const unsigned char*)a - *(const unsigned char*)b;
}
int back_comparator();
